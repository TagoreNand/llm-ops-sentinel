name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-22.04  # Using Ubuntu 22.04 for Python 3.11 support
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y python3.11-dev python3.11-distutils gcc g++ build-essential
    
    - name: Install Python dependencies
      run: |
        python3.11 -m pip install --upgrade pip setuptools wheel cython
        python3.11 -m pip install numpy==1.24.3
        python3.11 -m pip install hdbscan==0.8.33
        python3.11 -m pip install -r requirements.txt
    
    - name: Run tests
      run: |
        python3.11 -m pytest tests/ -v
